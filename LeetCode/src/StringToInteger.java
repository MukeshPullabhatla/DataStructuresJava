
public class StringToInteger {
	public int myAtoi(String s) {
		int len = s.length();
		int cur = 0;
		//Step 1: ignore the leading white spaces
		for(;cur<len && s.charAt(cur) == ' ', ++cur);
				//Step 2: sign
				int sign = 1;
				if(cur < len && ())
	}

}
